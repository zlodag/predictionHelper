/*
All prediction entries should be submitted via the multiple prediction entry form, which handles encryption.
This removes the single prediction entry form displayed on the home page.
*/

document.querySelector("form#new_prediction").remove();